#!/bin/bash

# System aliases
alias ll='ls -l'
alias lt='ls -altr'

# Drush aliases
alias dcc='drush cc all'
alias dfl='drush fl'
alias dfle='drush fl | grep -i enabled'
alias dfd='drush fd'
alias sqlc='drush sqlc'

# Front-end Dev aliases
alias guard='bundle install && bundle exec guard'
